{
  "name": "aia-runtime",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local-first IPC bridge, SQLite vault, and GHL webhook ingestion for AIA",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/sandboxWorkerEntry.js dist/sandboxWorkerEntry.js && mkdir -p dist/data && cp src/data/card_ui.json dist/data/card_ui.json",
    "dev": "tsx src/server.ts",
    "start": "node dist/server.js",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.9.1",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^4.17.21",
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
